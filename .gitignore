runs/
*.raw
__pycache__/
experiments/*/out/
decks/*/out/
.venv/
.ruff_cache/
.pytest_cache/
.sx/platform
# agent/session worktrees are real git checkouts INSIDE the repo; `git add -A` would sweep them in
# as embedded-repo gitlinks that mean nothing to a cloner (template#33)
.claude/worktrees/
