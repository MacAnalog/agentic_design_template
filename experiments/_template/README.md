# NNN — <technique>

<!-- `make lint` matches the bold row labels below (the set is `experiments_rows` in
     harness.yaml); a heading of the same name does not count. `run.py` simulates into out/,
     figs/ and tables/; `mk_readme.py` regenerates this file from out/*.json. Edit the
     generator, not the generated prose. -->

**Phase:** <system | topology | sizing | improve | layout — see CLAUDE.md, "How the work is organized">
**Paper(s):** <handles from references/INDEX.md, or none>
**Hypothesis:** <falsifiable: what moves, by how much, measured how>
**Control:** <what is held or re-allocated so the effect is attributable>
**Verdict:** IN PROGRESS

## Results

<tables from the ledger; prose is interpretation only>

## Evidence

| claim | figure or table |
|---|---|
| <the claim this experiment settles> | `figs/<name>.png` |
| <the numbers behind it> | `tables/<name>.csv` |

## Lessons to graduate

<entries for doc/journal/ at close-out, each with its ledger tag>
