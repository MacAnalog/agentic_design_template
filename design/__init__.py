"""The design's design. Generic, imported as-is: `sim` (the ngspice lane), `stimulus` (PRBS/PAM4/PWL),
`eye` (symbol-aware eye metrics + BT4 receiver), `exp` (labelled batches, markdown), `plot` (spec
boxes). Design-specific, filled per repo: `dut` (the sizing point -> deck), `metrics` (measure,
check, log)."""
